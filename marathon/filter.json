{
    "description": "Parallel script execution runtime that automatically discovers and runs generator scripts to automate the creation of content at scale.",
    "filters": [
        {
            "runWith": "deno",
            "script": "./marathon.ts"
        }
    ]
}
